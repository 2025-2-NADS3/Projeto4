<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissões de Internet e Galeria -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projeto_entrega2"
        tools:targetApi="31">

        <!-- CORREÇÃO: AuthSelectionActivity agora é a tela de início -->
        <activity
            android:name=".AuthSelectionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Outras activities -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".VitrineActivity" />
        <activity android:name=".CadastroActivity" />
        <activity android:name=".ProductDetailActivity" />
        <activity android:name=".ProductListActivity" />
        <activity android:name=".CarrinhoActivity" />
        <activity android:name=".CheckoutActivity" /> 
        <activity android:name=".CantinaPainelActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".CouponsActivity" />
        <activity android:name=".OrdersActivity" />
        <activity android:name=".ProfileActivity" />

    </application>

</manifest>
